<UserControl x:Class="GuardianEye.Admin.Views.Pages.TimeRequestsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuardianEye.Admin.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Time Requests" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="White" 
                   Grid.Row="0"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <ComboBox Width="150" Height="30" FontSize="14">
                <ComboBoxItem Content="All Requests" IsSelected="True"/>
                <ComboBoxItem Content="Pending"/>
                <ComboBoxItem Content="Approved"/>
                <ComboBoxItem Content="Denied"/>
            </ComboBox>
        </StackPanel>
        
        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="#FF2D2D30"
                  Foreground="White"
                  BorderBrush="#FF555555"
                  BorderThickness="1"
                  RowHeight="40"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="150"/>
                <DataGridTextColumn Header="Requested (min)" Binding="{Binding RequestedMinutes}" Width="100"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Requested At" Binding="{Binding RequestedAt}" Width="150"/>
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Approve" Margin="2" Padding="8,4" FontSize="12" Background="#FF107C10" Foreground="White"/>
                                <Button Content="Deny" Margin="2" Padding="8,4" FontSize="12" Background="#FFD13438" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>