<UserControl x:Class="GuardianEye.Admin.Views.Pages.DevicesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GuardianEye.Admin.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Device Status" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="White" 
                   Grid.Row="0" 
                   Margin="0,0,0,10"/>
        
        <TextBlock x:Name="LastRefreshText" 
                   Text="Last refresh: --" 
                   FontSize="12" 
                   Foreground="#FF888888"
                   Grid.Row="1"
                   HorizontalAlignment="Left"/>
        
        <DataGrid x:Name="DevicesDataGrid" 
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="#FF2D2D30"
                  Foreground="White"
                  BorderBrush="#FF555555"
                  BorderThickness="1"
                  RowHeight="40"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Device Name" Binding="{Binding DeviceName}" Width="*"/>
                <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="2*"/>
                <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="120"/>
                <DataGridTemplateColumn Header="State" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding StateColor}" CornerRadius="10" Padding="5,2">
                                <TextBlock Text="{Binding State}" Foreground="White" HorizontalAlignment="Center" FontSize="12"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Session Time" Binding="{Binding SessionRemaining}" Width="100"/>
                <DataGridTextColumn Header="Last Heartbeat" Binding="{Binding LastHeartbeat}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>