<Window x:Class="GuardianEye.Admin.Views.DashboardShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GuardianEye.Admin.Views"
        xmlns:views="clr-namespace:GuardianEye.Admin.Views.Pages"
        mc:Ignorable="d"
        Title="GuardianEye Admin Dashboard" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="#FF2D2D30" CornerRadius="20" Margin="10">
        <DockPanel LastChildFill="True">
            <!-- Sidebar -->
            <Border DockPanel.Dock="Left" Width="250" Background="#FF1E1E20" CornerRadius="15,0,0,15">
                <StackPanel Margin="0,20">
                    <TextBlock Text="GuardianEye Admin" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    <ListView x:Name="NavigationListView" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16" SelectionChanged="NavigationListView_SelectionChanged">
                        <ListViewItem x:Name="DashboardItem" Content="Dashboard" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="StudentsItem" Content="Students" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="MonitoringItem" Content="Monitoring" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="RestrictionsItem" Content="Restrictions" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="ReportsItem" Content="Reports" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="DevicesItem" Content="Devices" Height="50" Padding="20,0"/>
                        <ListViewItem x:Name="SettingsItem" Content="Settings" Height="50" Padding="20,0"/>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Border DockPanel.Dock="Right" Background="#FF2D2D30" CornerRadius="0,15,15,0" Padding="20">
                <ContentControl x:Name="MainContentControl"/>
            </Border>
        </DockPanel>
    </Border>
</Window>