<Window x:Class="GuardianEye.Views.AdminDashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:GuardianEye.ViewModels"
        mc:Ignorable="d"
        Title="GuardianEye - Admin Dashboard" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:AdminDashboardViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#1E1E2F">
            <StackPanel>
                <TextBlock Text="GuardianEye" FontSize="24" FontWeight="Bold" 
                           Foreground="#00D4FF" Margin="20,20,20,30"/>
                <Button Content="Dashboard" Style="{StaticResource SidebarButton}" Margin="10,0"/>
                <Button Content="Students" Style="{StaticResource SidebarButtonSelected}" Margin="10,0"/>
                <Button Content="Sessions" Style="{StaticResource SidebarButton}" Margin="10,0"/>
                <Button Content="Reports" Style="{StaticResource SidebarButton}" Margin="10,0"/>
                <Button Content="Settings" Style="{StaticResource SidebarButton}" Margin="10,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding WelcomeMessage}" Foreground="#CCCCCC" 
                           VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Button Content="Logout" Style="{StaticResource SmallButton}"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#2D2D44" CornerRadius="10" Margin="0,20,0,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Students" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text=" (Total: {Binding TotalStudents})" Foreground="#888888" 
                                   Margin="10,0,0,0" VerticalAlignment="Center"/>
                        <Button Content="Add Student" Command="{Binding AddStudentCommand}" 
                                Style="{StaticResource SmallButton}" Margin="20,0,0,0"/>
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Students}" 
                              SelectedItem="{Binding SelectedStudent}"
                              AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                            <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="100"/>
                            <DataGridTextColumn Header="Sessions" Binding="{Binding SessionsUsedToday}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding IsLoggedIn}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>